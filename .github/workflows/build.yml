name: Build plugin

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Package plugin zip
        run: |
          mkdir -p out/decky-launch-options/dist
          cp plugin.json package.json main.py README.md LICENSE out/decky-launch-options/
          cp dist/index.js out/decky-launch-options/dist/
          cd out && zip -r decky-launch-options.zip decky-launch-options

      - uses: actions/upload-artifact@v4
        with:
          name: decky-launch-options
          path: out/decky-launch-options.zip

      - name: Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: out/decky-launch-options.zip
